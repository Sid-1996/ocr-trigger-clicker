# 自動更新功能 — 實作計畫

## 目標

1. 啟動 3 秒後自動檢查新版本（可從設定關閉）
2. 發現新版本 → 對話框（版本資訊 + release notes +「不再提醒」checkbox）
3. 使用者確認 → 下載進度條 → 解壓縮 → 驗證 EXE → 批次腳本自我替換 + 重啟
4. 系統托盤選單 + 工具列提供手動「檢查更新」按鈕
5. 新版本首次啟動顯示「已更新至 vX.X.X」訊息

---

## 檔案變更

### 新增

| 檔案 | 說明 |
|---|---|
| `core/12_updater.py` | 自動更新核心邏輯（僅 stdlib + `_loader.py`，零新 dependency） |

### 修改

| 檔案 | 變更內容 |
|---|---|
| `gui/06_gui_main.py` | import `_updater`、新增 UI 元素、重寫 `_check_version`、新增進度/完成對話框、更新完成提示 |
| `build.py` | `py_datas` 加入 `core/12_updater.py` |

---

## `core/12_updater.py` 完整設計

```python
"""Auto-update: version check, download, self-replace, restart."""

import hashlib
import json
import logging
import os
import re
import shutil
import subprocess
import sys
import tempfile
import zipfile
from dataclasses import dataclass
from pathlib import Path
from urllib.request import Request, urlopen

log = logging.getLogger(__name__)

GITHUB_API_LATEST = (
    "https://api.github.com/repos/Sid-1996/ocr-trigger-clicker/releases/latest"
)
ASSET_NAME = "ocr-trigger-clicker.zip"
_USER_AGENT = "ocr-trigger-clicker-updater/1.0"


# ── 資料類別 ──────────────────────────────────────────────

@dataclass
class UpdateInfo:
    version: str         # "0.0.5"（已去 v 前綴）
    download_url: str    # GitHub CDN 下載網址
    release_url: str     # Release 頁面
    release_notes: str   # Markdown 內容


# ── 工具函式 ──────────────────────────────────────────────

def _parse_version(v: str) -> tuple[int, ...]:
    """'v0.0.5' 或 '0.0.5' → (0, 0, 5)。處理 dev 後綴（0.0.5-dev → (0, 0, 5)）。"""
    v = v.strip().lstrip("v")
    if not v:
        return (0,)
    parts = []
    for x in v.split("."):
        m = re.match(r"(\d+)", x)
        parts.append(int(m.group(1)) if m else 0)
    return tuple(parts)


def is_frozen() -> bool:
    """PyInstaller bundle 環境？"""
    return getattr(sys, "frozen", False)


def current_exe_path() -> Path:
    """目前執行中的 EXE 完整路徑（僅 frozen 時有意義）。"""
    return Path(sys.executable).resolve()


# ── 1. 版本檢查 ──────────────────────────────────────────

def check_for_update(current_version: str) -> UpdateInfo | None:
    """呼叫 GitHub Releases API，比對版本。

    Returns:
        UpdateInfo（有新版本）或 None（已是最新）
    Raises:
        RuntimeError: API 失敗、資產不存在
    """
    req = Request(GITHUB_API_LATEST, headers={"User-Agent": _USER_AGENT})
    with urlopen(req, timeout=10) as resp:
        data = json.loads(resp.read().decode("utf-8"))

    tag = data.get("tag_name", "")  # "v0.0.5"
    latest = _parse_version(tag)
    current = _parse_version(current_version)

    if latest <= current:
        return None

    assets = data.get("assets", [])
    zip_asset = next((a for a in assets if a["name"] == ASSET_NAME), None)
    if not zip_asset:
        raise RuntimeError(f"Release {tag} 沒有 {ASSET_NAME} 附件")

    return UpdateInfo(
        version=tag.lstrip("v"),
        download_url=zip_asset["browser_download_url"],
        release_url=data.get("html_url", ""),
        release_notes=data.get("body", ""),
    )


# ── 2. 下載 + 解壓 + 驗證 ──────────────────────────────

def download_update(
    info: UpdateInfo,
    progress_cb=None,
    cancel_event=None,
) -> Path:
    """下載 ZIP → 解壓 → 驗證 PE header。

    Args:
        info: UpdateInfo
        progress_cb: callable(downloaded_bytes, total_bytes) or None
        cancel_event: threading.Event — set 時中斷下載

    Returns:
        解壓後的 EXE 路徑

    Raises:
        RuntimeError: 網路錯誤、ZIP 損壞、PE 驗證失敗、使用者取消
    """
    tmp_dir = Path(tempfile.mkdtemp(prefix="ocr_update_"))
    zip_path = tmp_dir / ASSET_NAME
    exe_path = tmp_dir / "ocr-trigger-clicker.exe"

    try:
        # 下載（串流，64KB chunks）
        req = Request(info.download_url, headers={"User-Agent": _USER_AGENT})
        with urlopen(req, timeout=60) as resp:
            total = int(resp.headers.get("Content-Length", 0))
            downloaded = 0
            with open(zip_path, "wb") as f:
                while True:
                    if cancel_event and cancel_event.is_set():
                        raise RuntimeError("使用者取消下載")
                    chunk = resp.read(65536)
                    if not chunk:
                        break
                    f.write(chunk)
                    downloaded += len(chunk)
                    if progress_cb:
                        progress_cb(downloaded, total)

        # 解壓縮
        with zipfile.ZipFile(zip_path, "r") as zf:
            exe_entries = [n for n in zf.namelist() if n.endswith(".exe")]
            if not exe_entries:
                raise RuntimeError("ZIP 內無 .exe 檔案")
            # 優先根目錄下的 EXE，否則取第一個
            target = next(
                (n for n in exe_entries
                 if "/" not in n and "\\" not in n),
                exe_entries[0],
            )
            with zf.open(target) as src, open(exe_path, "wb") as dst:
                shutil.copyfileobj(src, dst)

        # 驗證 PE 標頭（"MZ"）
        with open(exe_path, "rb") as f:
            if f.read(2) != b"MZ":
                raise RuntimeError("下載檔案不是有效的 EXE（PE 標頭錯誤）")

        return exe_path

    except Exception:
        shutil.rmtree(tmp_dir, ignore_errors=True)
        raise


# ── 3. 自我替換 + 重啟 ─────────────────────────────────

def apply_update(new_exe_path: Path) -> Path:
    """產生批次腳本 → 啟動（detached）→ 回傳腳本路徑。

    批次腳本流程：
        1. 等待舊程序退出（2 秒）
        2. 嘗試 copy /Y，失敗則重試（最多 5 次，間隔 2 秒）
        3. 成功 → start 新 EXE
        4. 刪除自身

    Args:
        new_exe_path: download_update() 回傳的新 EXE 路徑

    Returns:
        批次腳本的路徑（呼叫端在 quit 前不需做額外動作）

    Raises:
        RuntimeError: 非 frozen 環境
    """
    if not is_frozen():
        raise RuntimeError("原始碼模式不支援自動更新")

    old_exe = current_exe_path()
    old_dir = old_exe.parent

    # 路徑脫逸（批次腳本中 % → %%）
    old_str = str(old_exe).replace("%", "%%")
    new_str = str(new_exe_path).replace("%", "%%")

    bat_path = new_exe_path.parent / "_update.bat"
    bat_content = f"""@echo off
setlocal
set "OLD={old_str}"
set "NEW={new_str}"
set COUNT=0

:retry
timeout /t 2 /nobreak >nul
copy /Y "%NEW%" "%OLD%" >nul 2>&1
if %errorlevel% equ 0 goto done
set /a COUNT+=1
if %COUNT% lss 5 goto retry

echo [ERROR] 更新失敗：無法取代舊檔案。
echo 請手動複製：
echo   %NEW%
echo 到：
echo   %OLD%
echo。
echo 若檔案位於受保護目錄，請以系統管理員身分執行。
pause
exit /b 1

:done
start "" "%OLD%"

:: 清理暫存目錄
rmdir /s /q "{new_exe_path.parent}" >nul 2>&1
del "%~f0"
"""
    # 使用 UTF-8 with BOM 確保中文路徑正確
    bat_path.write_text("\ufeff" + bat_content, encoding="utf-8-sig")

    subprocess.Popen(
        ["cmd", "/c", str(bat_path)],
        cwd=str(old_dir),
        creationflags=0x00000008,  # DETACHED_PROCESS
        close_fds=True,
    )
    return bat_path


# ── 4. Self-check（專案慣例）───────────────────────────

def demo():
    """在終端機執行版本比對測試（不涉及網路）。"""
    test_cases = [
        ("0.0.4", "0.0.4", False),
        ("0.0.4", "0.0.5", True),
        ("0.0.4", "v0.0.5", True),
        ("0.0.4", "0.0.5.1", True),
        ("0.0.4", "0.0.4.1", True),
        ("0.1.0", "0.0.9", False),
        ("", "", False),
        ("0.0.5-dev", "0.0.4", False),   # dev build should not trigger update
        ("0.0.5a1", "0.0.5", False),
    ]
    for cur, lat, expect in test_cases:
        result = _parse_version(lat) > _parse_version(cur)
        assert result == expect, (
            f"FAIL: {cur=} {lat=} expect={expect} got={result}"
        )
    print("✓ _parse_version: 全部通過")


if __name__ == "__main__":
    demo()
```

---

## `gui/06_gui_main.py` 變更細節

### 變更 1：Module-level import（補在 `06_gui_main.py:2283` 之後）

```python
# ── updater ──（與其他 core 模組相同位置）
_updater_mod = load_sibling("updater", "core/12_updater.py")
```

同時在檔案頂部的 `PyQt6.QtWidgets` import 區（`06_gui_main.py:25-56`）補上 `QProgressDialog`：

```python
from PyQt6.QtWidgets import (
    ...
    QProgressDialog,  # 新增
    ...
)
```

（其他函式不需要 `extract()`，因 GUI 直接呼叫 `_updater_mod.xxx()` 即可，不需要拆出模組層級變數。）

### 變更 2：`MainWindow.__init__` 補初始值（在 `06_gui_main.py:2530` 區域）

```python
self._updating = False       # 防止重複觸發更新檢查
self._downloading = False    # 防止重複下載
self._update_cancel = None   # threading.Event，供取消下載用
self._pending_update_ver = ""  # 當前下載的版本號
```

### 變更 3：工具列新增「檢查更新」按鈕（在 `06_gui_main.py:2736` 附近）

補在 `_simplified_btn` 之前：

```python
# ── 檢查更新 ──
self._update_btn = QPushButton("🔄檢查更新")
self._update_btn.setToolTip("檢查 GitHub 是否有新版本釋出")
self._update_btn.clicked.connect(lambda: self._check_version(force=True))
toolbar.addWidget(self._update_btn)
```

### 變更 4：系統托盤選單新增「檢查更新」（在 `06_gui_main.py:2590` 附近）

```python
_tray_menu.addAction("設定...", self._open_settings)
_tray_menu.addAction("檢查更新", lambda: self._check_version(force=True))  # 新增
_tray_menu.addSeparator()
```

### 變更 5：`SettingsDialog` 補「啟動時檢查更新」選項（`06_gui_main.py:2374`）

```python
# 在 __init__ 中，show_confirm checkbox 之後加入：
self._auto_check = QCheckBox("啟動時自動檢查更新")
self._auto_check.setChecked(not config.get("skip_update_check", False))
form.addRow("", self._auto_check)
```

```python
# _on_accept 補：
config["skip_update_check"] = not self._auto_check.isChecked()
```

### 變更 6：重寫 `_check_version()`（取代 `06_gui_main.py:5454`）

```python
def _check_version(self, force: bool = False):
    """檢查版本更新。force=True 時忽略 skip_update_check。"""
    if self._updating:
        return
    if not force:
        config = self._load_config()
        if config.get("skip_update_check", False):
            return

    self._updating = True
    self._update_cancel = threading.Event()
    self._status_bar.showMessage("正在檢查更新...")

    class _CheckWorker(QThread):
        result = pyqtSignal(object)   # UpdateInfo | None
        error = pyqtSignal(str)

        def run(self):
            try:
                info = _updater_mod.check_for_update(__version__)
                self.result.emit(info)
            except Exception as e:
                self.error.emit(str(e))

    self._check_worker = _CheckWorker()
    self._check_worker.result.connect(self._on_update_checked)
    self._check_worker.error.connect(self._on_update_error)
    self._check_worker.start()


def _on_update_checked(self, info):
    self._updating = False
    if info is None:
        self._status_bar.showMessage("已是最新版本", 3000)
        return

    cb = QCheckBox("不再提醒此次版本的更新")
    box = QMessageBox(self)
    box.setIcon(QMessageBox.Icon.Information)
    box.setWindowTitle("發現新版本")
    box.setText(f"新版本 v{info.version} 已發布（目前 v{__version__}）")
    if info.release_notes:
        box.setDetailedText(info.release_notes[:1000])
    box.setStandardButtons(
        QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No
    )
    box.button(QMessageBox.StandardButton.Yes).setText("立即更新")
    box.button(QMessageBox.StandardButton.No).setText("稍後再說")
    box.layout().addWidget(cb)

    if box.exec() != QMessageBox.StandardButton.Yes:
        if cb.isChecked():
            config = self._load_config()
            config["skip_update_check"] = True
            self._save_config(config)
        return

    if cb.isChecked():
        config = self._load_config()
        config["skip_update_check"] = True
        self._save_config(config)

    self._pending_update_ver = info.version
    self._start_download(info)


def _on_update_error(self, msg):
    self._updating = False
    self._status_bar.showMessage("")
    QMessageBox.warning(
        self,
        "檢查更新失敗",
        f"無法檢查更新：{msg}\n\n請前往 GitHub 手動下載最新版本。",
    )
```

### 變更 7：新增下載進度 + 完成對話框

```python
def _start_download(self, info):
    if self._downloading:
        return
    self._downloading = True
    self._update_cancel = threading.Event()

    self._progress = QProgressDialog("正在下載更新...", "取消", 0, 100, self)
    self._progress.setWindowTitle(f"下載 v{info.version}")
    self._progress.setWindowModality(Qt.WindowModality.WindowModal)
    self._progress.setMinimumDuration(0)
    self._progress.setValue(0)
    self._progress.canceled.connect(self._cancel_download)

    class _DownloadWorker(QThread):
        finished = pyqtSignal(object)
        error = pyqtSignal(str)
        progress = pyqtSignal(int, int)

        def run(self):
            try:
                exe_path = _updater_mod.download_update(
                    info,
                    progress_cb=lambda d, t: self.progress.emit(d, t),
                    cancel_event=self.parent()._update_cancel
                    if hasattr(self.parent(), "_update_cancel")
                    else None,
                )
                self.finished.emit(exe_path)
            except Exception as e:
                self.error.emit(str(e))

    self._dl_worker = _DownloadWorker()
    self._dl_worker.setParent(self)
    self._dl_worker.progress.connect(self._on_download_progress)
    self._dl_worker.finished.connect(self._on_download_finished)
    self._dl_worker.error.connect(self._on_download_error)
    self._dl_worker.start()


def _on_download_progress(self, downloaded, total):
    if total > 0:
        pct = int(downloaded * 100 / total)
        self._progress.setValue(pct)
        self._progress.setLabelText(
            f"正在下載更新...（{downloaded // 1024} KB / {total // 1024} KB, {pct}%）"
        )
    else:
        self._progress.setLabelText(
            f"正在下載更新...（{downloaded // 1024} KB，未知總大小）"
        )


def _cancel_download(self):
    if self._update_cancel:
        self._update_cancel.set()
    self._progress.close()
    self._downloading = False
    self._status_bar.showMessage("下載已取消", 3000)


def _on_download_finished(self, exe_path):
    self._progress.close()
    self._downloading = False

    # 寫入「更新完成」旗標（供下次啟動顯示）
    config = self._load_config()
    config["just_updated"] = self._pending_update_ver
    self._save_config(config)

    btn = QMessageBox.question(
        self,
        "更新下載完成",
        f"v{self._pending_update_ver} 已下載並驗證完成。\n"
        "是否立即重新啟動以套用更新？",
        QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No,
    )
    if btn == QMessageBox.StandardButton.Yes:
        try:
            _updater_mod.apply_update(exe_path)
            QApplication.quit()
        except Exception as e:
            QMessageBox.critical(
                self,
                "更新失敗",
                f"無法套用更新：{e}\n\n請手動下載並取代檔案。",
            )


def _on_download_error(self, msg):
    self._progress.close()
    self._downloading = False
    QMessageBox.critical(
        self,
        "下載失敗",
        f"下載更新時發生錯誤：{msg}\n\n請前往 GitHub 手動下載。",
    )
```

### 變更 8：更新完成提示（在 `MainWindow.__init__` 尾部）

```python
# 檢查「更新完成」旗標（在 _maybe_show_startup_guide 之前或之後）
config = self._load_config()
updated_ver = config.pop("just_updated", None)
if updated_ver:
    self._save_config(config)
    QTimer.singleShot(4000, lambda: (
        self._status_bar.showMessage(
            f"✅ 已更新至 v{updated_ver}！若有問題，請前往 GitHub 回報", 8000
        ),
        # 同時推送到 notification stack
        self._notif_stack.push(f"已更新至 v{updated_ver}"),
    ))
```

### 變更 9：保留既有 `_parse_version`（無變動）

`_parse_version()` at `06_gui_main.py:72` 維持不刪，`core/12_updater.py` 有自己的版本避免 circular import。

---

## `build.py` 變更

在 `py_datas` 列表（`build.py:69-85`）補一行：

```python
("core/12_updater.py", "core"),
```

---

## 邏輯檢漏清單（已回答之前 10 項 + 最終審計 3 項）

### 第一輪（計畫初版）

| # | 遺漏 | 解決 |
|---|---|---|
| 1 | Import 模式 | ✅ 使用 `load_sibling("updater", "core/12_updater.py")` |
| 2 | 批次腳本編碼 | ✅ `\ufeff` + `utf-8-sig` 寫入，支援中文路徑 |
| 3 | `_pending_version` | ✅ `self._pending_update_ver` 在 `_on_update_checked` 設定 |
| 4 | 更新完成提示 | ✅ `config["just_updated"]` 旗標，下次啟動顯示 |
| 5 | 批次腳本清理暫存 | ✅ `rmdir /s /q` 刪除 temp 目錄 |
| 6 | 下載取消清理 | ✅ `download_update()` 內 `try/finally` → `shutil.rmtree` |
| 7 | `build.py` 未更新 | ✅ 加入 `py_datas` |
| 8 | GUI 按鈕位置 | ✅ 工具列 + 系統托盤 + 設定對話框三處 |
| 9 | 權限不足處理 | ✅ 批次腳本檢測 `errorlevel`，5 次重試後顯示可讀訊息 |
| 10 | 缺少 self-check | ✅ `demo()` 函式測試 `_parse_version` |

### 最終審計（撰寫後檢查）

| # | 類別 | 發現 | 來源確認 | 解決 |
|---|---|---|---|---|
| 11 | 🐛 Bug | `_parse_version` 對 dev 後綴（`0.0.5-dev`→`(0,0,0)`）會錯誤觸發更新 | 實際執行 python 測試驗證 | ✅ 改用 `re.match(r"\d+", x)` 取前綴數字 |
| 12 | ♻️ 清理 | `import threading` 已在 `06_gui_main.py:6` 模組層級匯入，方法內重複 import | grep 確認 | ✅ 移除方法內的 `import threading` |
| 13 | ⚠️ 邊界 | CDN 可能不提供 `Content-Length`，進度條卡在 0% | 推測（GitHub CDN 行為） | ✅ `total==0` 時改顯示「未知總大小」文字 |

---

## 變更檔案摘要

| 檔案 | 變更類型 | 行數估計 |
|---|---|---|
| `core/12_updater.py` | **新增** | ~180 行 |
| `gui/06_gui_main.py` | 修改 | ~+120 行 / -20 行（取代舊 `_check_version`） |
| `build.py` | 修改 | +1 行 |

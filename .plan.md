# 後台模式座標轉換修復計畫

## 核心原則（不動的東西）
- 前景模式的所有邏輯（mss 截圖、pynput 點擊、ROI 座標轉換）完全不動
- 主循環的 `_resolve_roi` / `_resolve_point` / `_to_screen_coords` 完全不動
- 已有的規則 JSON 必須在前景模式下照常運作
- 使用者切換模式不需要重新設定規則

## 目標
同一份規則 JSON，前景跑正常，切到後台模式也正常。

---

## 座標系統參考

| 座標系統 | 說明 |
|----------|------|
| 螢幕絕對 | 螢幕上的絕對像素位置 |
| 視窗相對 | 相對於視窗左上角的像素 |
| 客戶區相對 | 相對於客戶區左上角的像素（扣除標題列+邊框） |
| 客戶區比例 | 0.0~1.0，除以客戶區尺寸 |
| 視窗比例 | 0.0~1.0，除以全視窗尺寸 |

關鍵常數：`chrome = get_window_client_offset(title)` = `(offset_x, offset_y)` = 視窗左上角到客戶區左上角的像素距離。

`capture_print_window_hwnd()` 產生**全視窗大小的圖片**（用 `GetWindowRect` 尺寸），客戶區內容在 chrome offset 位置，其餘為黑邊。

---

## Bug 清單

### Bug 1: ROI 選取器 (bg) — 高
**檔案**: `gui/screenshot_controller.py:68-87`

**問題**: `select_roi_bg()` 回傳視窗相對像素，但轉換邏輯假設是螢幕絕對座標，多減了 `wr["x"]` / `wr["y"]`。

**影響**: ROI 選取在 bg 模式下產生嚴重偏移的偵測區域。

**修復**: bg 模式直接將視窗相對像素轉為客戶區比例座標，不減 `wr["x"]`。

---

### Bug 2: 點擊座標選取器 (bg) — 中
**檔案**: `gui/06_gui_main.py:5029-5034`

**問題**: 除以全視窗尺寸得到視窗比例，但 `_ClickStepForm._pick_coord()` (行 1877) 和 `_DragStepForm._pick_coord()` (行 1963) 都標記為 `roi_coord:"client"`。主循環用客戶區尺寸計算，導致 `chrome*(1-ratio)` 像素偏移。

**影響**: 點擊座標在 bg 模式下有數像素偏移。

**修復**: 除以客戶區尺寸（先減 chrome offset）。

---

### Bug 3: 擷取區域 (bg) — 中
**檔案**: `gui/screenshot_controller.py:137-143`

**問題**: 同 Bug 2，除以全視窗尺寸但標記為 `roi_coord:"client"`。

**影響**: 模板比對 ROI 在 bg 模式下偏移。

**修復**: 除以客戶區尺寸（先減 chrome offset）。

---

### Bug 4: 主循環 `_handle_drag` (bg) — 高
**檔案**: `core/05_main_loop.py:769-776`

**問題**: `_to_screen_coords` 產生螢幕絕對座標，但 `_bg_input.drag()` 期望客戶區座標。`_send_click` 正確做了 `ScreenToClient` 轉換，但 `_handle_drag` 繞過了直接傳螢幕絕對座標。

**影響**: 拖曳操作在 bg 模式下起點/終點錯誤（偏移視窗螢幕位置）。

**修復**: 在呼叫 `_bg_input.drag()` 前做 `ScreenToClient` 轉換。

---

## 修復計畫

### 修復 1: `gui/screenshot_controller.py` — `open_roi_selector()` 方法

**改動範圍**: 只改 bg 分支，前景分支完全不動。

```python
# 現有代碼 (行 62-95):
if not result:
    return None
win._edit_stack.setCurrentIndex(1)
win._status_bar.showMessage(...)
if result.get("roi_coord") == "client":
    return result
# 以下全部是前景轉換邏輯，bg 模式也走到了 ← BUG

# 修復後:
if not result:
    return None
win._edit_stack.setCurrentIndex(1)
win._status_bar.showMessage(...)
if result.get("roi_coord") == "client":
    return result
# bg 模式：result 是 PrintWindow 圖片的像素座標（視窗相對）
# 直接轉為客戶區比例座標，不需要減去 wr["x"] / wr["y"]
if self._is_bg_mode():
    if title:
        wr = get_window_rect(title)
        if wr and wr["w"] > 0 and wr["h"] > 0:
            chrome = get_window_client_offset(title) or (0, 0)
            cx, cy = chrome
            client_w = wr["w"] - cx
            client_h = wr["h"] - cy
            if client_w > 0 and client_h > 0:
                result = {
                    "x": max(0.0, (result["x"] - cx) / client_w),
                    "y": max(0.0, (result["y"] - cy) / client_h),
                    "w": min(1.0, result["w"] / client_w),
                    "h": min(1.0, result["h"] / client_h),
                    "roi_coord": "client",
                }
            else:
                result = {
                    "x": result["x"] / wr["w"],
                    "y": result["y"] / wr["h"],
                    "w": result["w"] / wr["w"],
                    "h": result["h"] / wr["h"],
                }
        return result
    # 前景模式：result 是螢幕絕對座標（現有邏輯完全不動）
    if title:
        wr = get_window_rect(title)
        if wr:
            result["x"] -= wr["x"]
            result["y"] -= wr["y"]
    if title and wr and wr["w"] > 0 and wr["h"] > 0:
        chrome = get_window_client_offset(title) or (0, 0)
        cx, cy = chrome
        client_w = wr["w"] - cx
        client_h = wr["h"] - cy
        if client_w > 0 and client_h > 0:
            result = {
                "x": max(0.0, (result["x"] - cx) / client_w),
                "y": max(0.0, (result["y"] - cy) / client_h),
                "w": min(1.0, result["w"] / client_w),
                "h": min(1.0, result["h"] / client_h),
                "roi_coord": "client",
            }
        else:
            result = {
                "x": result["x"] / wr["w"],
                "y": result["y"] / wr["h"],
                "w": result["w"] / wr["w"],
                "h": result["h"] / wr["h"],
            }
    return result
```

---

### 修復 2: `gui/06_gui_main.py` — `_on_pick_coord()` 方法

**改動範圍**: 只改 bg 分支，前景分支完全不動。

```python
# 現有代碼 (行 5029-5035):
# result is (x, y) in client-area pixels
h, w = img.shape[:2]
self._edit_stack.setCurrentIndex(1)
self._status_bar.showMessage(T("notif.coordinate_selected", x=result[0], y=result[1]))
if w > 0 and h > 0:
    return (result[0] / w, result[1] / h)  # ← BUG: 除以全視窗尺寸
return (0.0, 0.0)

# 修復後:
h, w = img.shape[:2]
self._edit_stack.setCurrentIndex(1)
self._status_bar.showMessage(T("notif.coordinate_selected", x=result[0], y=result[1]))
if w > 0 and h > 0:
    chrome = get_window_client_offset(title) or (0, 0)
    cx, cy = chrome
    wr = get_window_rect(title) if title else None
    if wr and wr["w"] > cx and wr["h"] > cy:
        client_w = wr["w"] - cx
        client_h = wr["h"] - cy
        if client_w > 0 and client_h > 0:
            return (
                max(0.0, (result[0] - cx) / client_w),
                max(0.0, (result[1] - cy) / client_h),
            )
    return (result[0] / w, result[1] / h)
return (0.0, 0.0)
```

---

### 修復 3: `gui/screenshot_controller.py` — `open_capture_region()` 方法

**改動範圍**: 只改 bg 分支的 `roi_ratio` 計算。

```python
# 現有代碼 (行 137-143):
roi_ratio = {
    "x": rect["x"] / img.shape[1] if img.shape[1] > 0 else 0.0,
    "y": rect["y"] / img.shape[0] if img.shape[0] > 0 else 0.0,
    "w": rect["w"] / img.shape[1] if img.shape[1] > 0 else 0.0,
    "h": rect["h"] / img.shape[0] if img.shape[0] > 0 else 0.0,
    "roi_coord": "client",
}

# 修復後:
chrome = get_window_client_offset(title) or (0, 0) if title else (0, 0)
cx, cy = chrome
wr = get_window_rect(title) if title else None
if wr and wr["w"] > cx and wr["h"] > cy:
    client_w = wr["w"] - cx
    client_h = wr["h"] - cy
    if client_w > 0 and client_h > 0:
        roi_ratio = {
            "x": max(0.0, (rect["x"] - cx) / client_w),
            "y": max(0.0, (rect["y"] - cy) / client_h),
            "w": min(1.0, rect["w"] / client_w),
            "h": min(1.0, rect["h"] / client_h),
            "roi_coord": "client",
        }
    else:
        roi_ratio = {
            "x": rect["x"] / wr["w"] if wr["w"] > 0 else 0.0,
            "y": rect["y"] / wr["h"] if wr["h"] > 0 else 0.0,
            "w": rect["w"] / wr["w"] if wr["w"] > 0 else 0.0,
            "h": rect["h"] / wr["h"] if wr["h"] > 0 else 0.0,
            "roi_coord": "client",
        }
else:
    roi_ratio = {
        "x": rect["x"] / img.shape[1] if img.shape[1] > 0 else 0.0,
        "y": rect["y"] / img.shape[0] if img.shape[0] > 0 else 0.0,
        "w": rect["w"] / img.shape[1] if img.shape[1] > 0 else 0.0,
        "h": rect["h"] / img.shape[0] if img.shape[0] > 0 else 0.0,
        "roi_coord": "client",
    }
```

---

### 修復 4: `core/05_main_loop.py` — `_handle_drag()` 方法

**改動範圍**: 只改 bg 分支，前景分支完全不動。

```python
# 現有代碼 (行 774-776):
if mode and mode != "pynput" and self._window_hwnd:
    _bg_input.set_method(mode)
    ok = _bg_input.drag(self._window_hwnd, ssx, ssy, sex, sey, button)

# 修復後:
if mode and mode != "pynput" and self._window_hwnd:
    _bg_input.set_method(mode)
    import ctypes
    user32 = ctypes.windll.user32
    pt1 = wintypes.POINT(ssx, ssy)
    user32.ScreenToClient(self._window_hwnd, ctypes.byref(pt1))
    pt2 = wintypes.POINT(sex, sey)
    user32.ScreenToClient(self._window_hwnd, ctypes.byref(pt2))
    ok = _bg_input.drag(self._window_hwnd, pt1.x, pt1.y, pt2.x, pt2.y, button)
```

---

## 修改摘要

| 檔案 | 函式 | 改法 |
|------|------|------|
| `gui/screenshot_controller.py` | `open_roi_selector()` | Bug 1: bg 模式跳過 `wr["x"]` / `wr["y"]` 減法 |
| `gui/06_gui_main.py` | `_on_pick_coord()` | Bug 2: bg 模式除以客戶區尺寸 |
| `gui/screenshot_controller.py` | `open_capture_region()` | Bug 3: bg 模式除以客戶區尺寸 |
| `core/05_main_loop.py` | `_handle_drag()` | Bug 4: 加 `ScreenToClient` 轉換 |

**不改動的東西：**
- `gui/17_bg_roi_selector.py` — 回傳值格式不變
- `gui/18_bg_click_picker.py` — 回傳值格式不變
- `gui/09_ocr_debug.py` — 已正確
- `gui/test_run_controller.py` — 已正確
- `core/05_main_loop.py` 的 `_resolve_roi` / `_resolve_point` / `_send_click` / `_to_screen_coords` — 不動
- 前景模式所有邏輯 — 不動

---

## 驗證方式

1. **前景模式 ROI 選取**：選取 ROI → 檢查規則 JSON 中的 `roi_coord:"client"` 和座標值
2. **後台模式 ROI 選取**：切換到 postmessage 模式 → 選取 ROI → 檢查規則 JSON 中的 `roi_coord:"client"` 和座標值
3. **前景模式執行規則**：確保選取的 ROI 在前景模式下正確執行
4. **後台模式執行規則**：確保選取的 ROI 在後台模式下正確執行
5. **切換模式**：同一份規則在兩種模式下都能正確執行
6. **拖曳操作**：後台模式下拖曳起點/終點正確

---

## 實施步驟

1. 修改 `gui/screenshot_controller.py` 的 `open_roi_selector()` 和 `open_capture_region()`
2. 修改 `gui/06_gui_main.py` 的 `_on_pick_coord()`
3. 修改 `core/05_main_loop.py` 的 `_handle_drag()`
4. 執行 `ruff check --fix .` + `ruff format .`
5. 執行 `graphify update .`
6. Commit + push
